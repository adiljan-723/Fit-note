# Fit Note

当前版本：`v0.9.0`

面向健身房新手和入门用户的微信小程序，支持按增肌/减脂目标生成训练计划、执行今日训练、查看动作库、学习动作要点、查看动作图解占位、逐组记录训练、记录饮食，并通过首页查看今日任务和完成情况。

## v0.9.0 已实现功能

- 首页：今日训练仪表盘、欢迎语、今日训练进度、今日完成度、本周训练数据、饮食状态、动作库快捷入口
- 计划页：增肌计划、减脂计划、生成计划、我自有计划、动作部位选择、动作建议自动匹配
- 今日训练页：读取当前目标的今日计划，展示动作列表、完成进度、动作记录状态，并支持查看动作详情
- 训练页：每组单独记录重量和次数，支持添加一组、删除一组、复制上一组、复制上次同动作训练记录
- 饮食页：根据体重和目标估算热量目标、蛋白质目标和饮水目标，支持快捷饮水
- 动作库页：按胸部、背部、腿臀、肩部、手臂、核心、有氧分类查看动作，支持搜索
- 动作详情页：展示动作介绍、训练目标、动作步骤、训练要点、常见错误和新手建议
- 我的页：设置基础资料，展示 CloudBase 用户身份和云端同步状态，并提供手动同步入口
- 云开发：已在 `app.js` 初始化 CloudBase 环境，已创建 `login` 云函数，已接入云数据库集合和本地 + 云端同步基础能力

## CloudBase 云开发信息

当前已连接 CloudBase 云开发环境：

```text
环境 ID：ai-native-d2g6w34udacbb193d
区域：ap-shanghai
状态：NORMAL
```

当前版本已完成第一阶段后端落地，使用本地优先 + 云端同步方式保存核心数据：

```text
users              云函数维护的用户登录信息
user_profiles      用户资料
training_records   训练记录
diet_records       饮食记录
workout_plans      训练计划
checkins           打卡记录
```

说明：训练草稿仍仅保存在本地，用于页面间临时传递。

## v0.9.0 相比 v0.8.0 的重点变化

- 新增 `cloudfunctions/login` 云函数，用于获取微信小程序用户 `openid`
- 新增 `users`、`user_profiles`、`training_records`、`diet_records`、`workout_plans`、`checkins` 云数据库集合规划和权限配置
- 在 `utils/storage.js` 中加入本地 + 云端同步层，尽量保持页面原有调用方式不变
- 训练记录、饮食记录、用户资料、训练计划和打卡变更后会自动尝试同步云端
- 我的页新增云端同步状态、用户标识和手动同步按钮

## 数据保存

当前版本采用本地优先策略：用户操作会先保存到微信小程序本地缓存，保证页面响应和离线可用；当 CloudBase 可用时，会自动把用户资料、训练记录、饮食记录、训练计划和打卡数据同步到云数据库。

如果云端同步失败，本地数据仍会保留。用户可在“我的”页面点击“同步”再次尝试。

## 如何运行

1. 安装微信开发者工具。
2. 打开微信开发者工具，选择“导入项目”。
3. 项目目录选择当前文件夹。
4. 当前 `project.config.json` 已配置测试号 AppID，如需发布请换成正式小程序 AppID。
5. 点击“编译”预览。

## 上传版本建议

在微信开发者工具点击“上传”时，版本号填写：

```text
0.9.0
```

项目备注建议填写：

```text
Fit Note v0.9.0：接入 CloudBase 用户身份、login 云函数、云数据库集合和本地 + 云端同步基础能力，完成第一阶段后端落地。
```

## 下一版建议

- 将当前客户端直连云数据库同步升级为统一 `syncUserData` 云函数
- 增加训练/饮食记录 CSV 导出云函数
- 增加云端训练统计聚合能力
- 完善删除、分页、冲突合并和多设备同步策略
- 为核心动作补充正式授权图解素材
